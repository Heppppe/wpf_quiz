<UserControl x:Class="Quiz_Solver_App.View.QuizSolver"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <!-- Pasek z pytaniem -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="{Binding QuestionText}" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Okienko z czasem -->
        <Border Grid.Row="1" Grid.Column="1" Margin="0,0,0,20" Padding="10"
                BorderBrush="Gray" BorderThickness="1" CornerRadius="5" HorizontalAlignment="Right">
            <TextBlock Text="{Binding TimeLeftDisplay}" FontSize="20" FontWeight="SemiBold"/>
        </Border>

        <!-- Odpowiedzi -->
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,20">
            <ItemsControl ItemsSource="{Binding Answers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <RadioButton Content="{Binding .}"
                                     GroupName="Answers"
                                     IsChecked="{Binding DataContext.SelectedAnswerIndex, 
                                         RelativeSource={RelativeSource AncestorType=UserControl}, 
                                         Converter={StaticResource IndexToIsCheckedConverter}, 
                                         ConverterParameter={Binding RelativeSource={RelativeSource Self}}}"
                                     Command="{Binding DataContext.SelectAnswerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                     CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=Items.CurrentPosition}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="1">
            <Button Content="Zakończ quiz" Command="{Binding EndQuizCommand}"/>

        </StackPanel>

        <!-- Nawigacja -->
        <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Poprzednie pytanie" Command="{Binding PreviousQuestionCommand}" Grid.Column="0" HorizontalAlignment="Left" Width="180"/>
            <Button Content="Następne pytanie" Command="{Binding NextQuestionCommand}" Grid.Column="1" HorizontalAlignment="Right" Width="180"/>
        </Grid>
    </Grid>
</UserControl>